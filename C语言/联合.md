## 联合

- 联合的所有成员引用的是**内存中的相同位置**。
- 每个成员所引用的位相同，仅有的区别在于每个成员的类型决定了这些位被如何解释。
- 联合的长度就是它最长成员的长度。

### 联合的初始化

```c
union {
    int a;
    float b;
    char c[4];
} x = { 5 };
```

联合变量可以被初始化，但这个初始值必须是联合第一个成员的类型，而且必须位于一对花括号里面。

如果给出的初始值是其他类型，它就会被隐式转换为第一个成员的类型。