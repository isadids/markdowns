## 动态内存分配

### `malloc`

> `malloc` 实际分配的内存有可能比请求的稍微多一点。但是，这个行为是由编译器定义的，所以不能指望它肯定会分配比请求多的内存。

### `realloc`

新内存默认不会初始化。

如果原内存块无法改变大小，`realloc` 会新分配内存，并原内存内存。

如果 `realloc` 的第一个参数是 `NULL`，其行为与 `malloc` 相同。

